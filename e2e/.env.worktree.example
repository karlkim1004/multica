# Disposable local E2E stack only. Copy to .env.worktree; do not put
# production credentials in this file. The default docker-compose.yml uses
# the same localhost values below.
E2E_BASE_URL=http://localhost:3000
E2E_API_URL=http://localhost:8080
E2E_DATABASE_URL=postgres://multica:multica@localhost:5432/multica?sslmode=disable
E2E_JWT_SECRET=multica-dev-secret-change-in-production
